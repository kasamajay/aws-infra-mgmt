version: 2.1

jobs:
  create-ec2:
    docker:
    - image: cimg/python:3.12
    steps:
    - run: 
        name: aws sts
        command: |
          aws sts get-caller-identity

workflows:
  main:
    jobs:
    - create-ec2:
        context: kasamajay-github
  
